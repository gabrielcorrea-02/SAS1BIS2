<%- include('partials/head', { title: 'Histórico de Relat�rios - 1°BIS(AMV)' })%>

<body class="bg-gray-100 font-sans text-gray-800">

<header class="bg-green-800 text-white shadow p-4 flex justify-between items-center">
  <h1 class="font-bold text-xl">Hist�rico de Relat�rios</h1>
  <a href="/dashboard" class="bg-green-600 px-3 py-1 rounded hover:bg-green-700">Voltar</a>
</header>

<div class="flex">
  <!-- Sidebar -->
  <%- include('partials/sidebar') %>
  <!-- Conte�do principal -->
  <main class="flex-1 p-6">
    <h2 class="text-xl font-bold mb-4">Relat�rios Gerados</h2>

    <% if(files.length === 0){ %>
      <p>Nenhum relat�rio gerado at� o momento.</p>
    <% } else { %>
      <div class="overflow-x-auto bg-white p-4 rounded shadow">
        <table class="w-full border-collapse">
          <thead>
            <tr class="bg-green-700 text-white">
              <th class="p-3 text-left">Arquivo</th>
              <th class="p-3 text-center">A��o</th>
            </tr>
          </thead>
          <tbody>
            <% files.forEach(file => { %>
              <tr class="border-b hover:bg-gray-50">
                <td class="p-3"><%= file.name %></td>
                <td class="p-3 text-center">
                  <a href="<%= file.url %>" class="bg-blue-600 text-white px-3 py-1 rounded hover:bg-blue-700" download>
                    Baixar
                  </a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </main>
</div>

  <%- include('partials/footer') %>


</body>
</html>
