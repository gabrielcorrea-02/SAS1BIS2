<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Sistema de Controle</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #6ba8e7 0%, #a0c4ff 100%);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .login-container {
            background: rgba(255, 255, 255, 0.98);
            border-radius: 25px;
            padding: 40px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
            width: 90%;
            max-width: 400px;
            text-align: center;
            position: relative;
            overflow: hidden;
            margin-bottom: 20px;
        }

        .login-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 8px;
            background: linear-gradient(90deg, #006400 0%, #008000 50%, #006400 100%);
        }

        .military-header {
            background: #006400;
            color: white;
            padding: 20px;
            margin: -40px -40px 30px -40px;
            border-radius: 25px 25px 0 0;
        }

        .military-header h1 {
            font-size: 1.4em;
            font-weight: bold;
            margin: 0;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.5);
        }

        .military-header h2 {
            font-size: 1.1em;
            font-weight: normal;
            margin: 5px 0 0 0;
            color: white;
            opacity: 0.95;
        }

        .logo {
            width: 100px;
            height: auto;
            margin-bottom: 10px;
}


        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            color: #333;
            font-weight: bold;
            font-size: 14px;
        }

        .form-group input {
            width: 100%;
            padding: 12px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 16px;
            transition: border-color 0.3s;
            font-family: Arial, sans-serif;
        }

        .form-group input:focus {
            outline: none;
            border-color: #006400;
            box-shadow: 0 0 5px rgba(0, 100, 0, 0.3);
        }

        .btn-login {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, #006400 0%, #008000 100%);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
            font-family: Arial, sans-serif;
            text-transform: uppercase;
        }

        .btn-login:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 100, 0, 0.4);
        }

        .btn-login:active {
            transform: translateY(0);
        }

        .error-message {
            color: #d32f2f;
            background: #ffebee;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 20px;
            border-left: 4px solid #d32f2f;
            font-size: 14px;
        }

        .footer {
            background: #556B2F;
            color: white;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            width: 90%;
            max-width: 410px;
            font-size: 0.9em;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            font-family: Arial, sans-serif;
        }

        @media (max-width: 480px) {
            .login-container {
                padding: 30px 20px;
                margin: 20px;
            }
            
            .military-header h1 {
                font-size: 1.2em;
            }
            
            .military-header h2 {
                font-size: 1em;
            }
            
            .footer {
                font-size: 0.8em;
                padding: 12px;
            }
        }
    </style>
</head>
<body>
    <img class="logo" src="/logo.png" alt="Logo do Batalhão">
    <div class="login-container">
        <div class="military-header">
            <h1>1°BIS(AMV)</h1>
            <h2>Sistema de Controle de Entrada e Saida</h2>
        </div>
        
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message">
                <%= error %>
            </div>
        <% } %>

        <form action="/login" method="POST">
            <div class="form-group">
                <label for="username">Usuario:</label>
                <input type="text" id="username" name="username" required autofocus>
            </div>
            
            <div class="form-group">
                <label for="password">Senha:</label>
                <input type="password" id="password" name="password" required>
            </div>
            
            <button type="submit" class="btn-login">ACESSAR SISTEMA</button>
        </form>
    </div>

    <div class="footer">
        Desenvolvido por Ten Asfora e Ten Cavalcante - IME/2025<br>
        XXIII Operação Ricardo Franco
    </div>
</body>
</html>